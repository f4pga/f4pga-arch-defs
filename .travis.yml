language: python
python:
 - "3.6"

addons:
  apt:
    packages:
     - colordiff
     - graphviz
     - inkscape
     - libxml2-utils
     - make
     - nodejs
     - xsltproc

before_install:
 - source ./.travis/common.sh

install:
 - ./.travis/install.sh

script:
 - ./.travis/script.sh

after_success:
 - ./.travis/after_success.sh

after_failure:
 - ./.travis/after_failure.sh
