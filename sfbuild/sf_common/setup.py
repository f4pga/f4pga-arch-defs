from setuptools import setup

setup(
    name = 'sf_common',
    version = '0.0.1',
    author = 'antmicro',
    packages = ['sf_common'],
    license = 'ISC',
    description = 'Common classes and functions for symbiflow and its modules',
    install_requires = []
)